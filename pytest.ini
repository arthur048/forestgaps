[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Coverage disabled for now - re-enable when pytest-cov is installed
# addopts = --cov=forestgaps --cov-report=term --cov-report=html 